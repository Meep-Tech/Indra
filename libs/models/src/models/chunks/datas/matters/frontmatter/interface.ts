import { IMatter } from "../matter/interface";

/**
 * The Yaml-like frontmatter of a mote. 
 * Used to store a mote's 
 * - Metadata, 
 * - Initial Param Arguments,
 * - and State.
 */
export interface IFrontmatter extends IMatter {

}